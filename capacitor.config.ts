import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.wani3000.bible",
  appName: "KorRV Bible",
  webDir: "out",
  ios: {
    contentInset: "automatic",
  },
};

export default config;
